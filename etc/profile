if [ -r /Users/Shared/dist/profile ]; then
  . /Users/Shared/dist/profile
else
  for f in $(find /Users/Shared/etc/profile.stub.d -mindepth 1 -maxdepth 1 -type f ! -name '.*' | sort); do
    . "$f"
  done
fi
